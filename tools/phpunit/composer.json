{
    "require": {
        "symfony/phpunit-bridge": "^7"
    },
    "scripts": {
        "post-install-cmd": [
            "@php vendor/bin/simple-phpunit install"
        ],
        "post-update-cmd": [
            "@php vendor/bin/simple-phpunit install"
        ]
    }
}
