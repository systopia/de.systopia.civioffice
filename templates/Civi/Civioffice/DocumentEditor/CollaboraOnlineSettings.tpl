<div class="crm-section">
  <div class="label">{$form.cool_url.label}</div>
  <div class="content">{$form.cool_url.html}</div>
  <div class="clear"></div>
  <div class="help">{ts}The URL to your Collabora Online system.{/ts}</div>
</div>
<div class="crm-section">
  <div class="label">{$form.wopi_src_hostname.label}</div>
  <div class="content">{$form.wopi_src_hostname.html}</div>
  <div class="clear"></div>
  <div class="help">{ts}This hostname will be used in the <code>WOPISrc</code> parameter instead of the CiviCRM hostname. Can be used for example when Collabora Online runs in a container on the same system and requires a different hostname/IP to access CiviCRM.{/ts}</div>
</div>
